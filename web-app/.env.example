# Google OAuth credentials
# Get these from https://console.cloud.google.com/apis/credentials
GOOGLE_CLIENT_ID=your-client-id.apps.googleusercontent.com
GOOGLE_CLIENT_SECRET=your-client-secret

# Claude API key for AI classification
# Get from https://console.anthropic.com
CLAUDE_API_KEY=sk-ant-...

# Next.js
NEXTAUTH_URL=http://localhost:3000
NEXTAUTH_SECRET=generate-a-random-secret-here

# Cron secret for hourly auto-sort
# Generate a random secret and set it in your Vercel environment variables
# This protects the cron endpoint from unauthorized access
#
# Setup with free cron service (cron-job.org):
# 1. Create account at https://cron-job.org
# 2. Create a new cron job with URL: https://your-app.vercel.app/api/cron/autosort
# 3. Set schedule to run every hour (0 * * * *)
# 4. Add header: Authorization: Bearer YOUR_CRON_SECRET
CRON_SECRET=generate-a-random-secret-here
